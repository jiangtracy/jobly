/**
 * Jobs
 *
 * props: 
 * - none
 * 
 * state: 
 * - an array of jobs like [{ id, title, salary, equity, company } ...]
 * where company is { handle, name, description, numEmployees, logoUrl } 
 * 
 * App -> Routes -> Jobs
 **/

function Jobs() {
  return (
    <div>
      Jobs 
    </div>
  )
}

export default Jobs;